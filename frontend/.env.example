# Base origin of the backend API. Leave UNSET for local dev (Vite proxies /api → :4000).
# In production set it to your deployed backend, with NO trailing slash:
VITE_API_URL=https://attendance-tracker-dc8m.onrender.com
